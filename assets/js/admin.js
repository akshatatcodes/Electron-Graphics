document.addEventListener('DOMContentLoaded', () => {
    // Fetch and display stats for the dashboard
    document.getElementById('userCount').innerText = '10'; // Example data
    document.getElementById('projectCount').innerText = '5'; // Example data
    document.getElementById('visitCount').innerText = '150'; // Example data
});
